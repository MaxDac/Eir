import { NameChangingDirectiveDirective } from './name-changing-directive.directive';

describe('NameChangingDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NameChangingDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
