export interface CharacterType {
  id: number;
  name: string;
  maxMartialAttributes: number | null;
  maxMentalAttributes: number | null;
  maxMartialAbilities: number | null;
  maxMentalAbilities: number | null;
}
